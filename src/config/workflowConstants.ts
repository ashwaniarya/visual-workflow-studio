export const VALID_NODE_TYPES_MAP = {
  START: 'START',
  TRANSFORM: 'TRANSFORM',
  IFELSE: 'IFELSE',
  SWITCH: 'SWITCH',
  END: 'END',
  DISPLAY: 'DISPLAY',
  URL_SHORTENER: 'URL_SHORTENER',
} as const

export const WORKFLOW_CONSTANTS = {
  MAX_EXECUTION_STEPS: 100,
  MAX_UNDO_REDO_HISTORY_STEPS: 50,
  REDO_SHORTCUT_KEYS: ['z', 'Z', 'y', 'Y'] as const,
  DEFAULT_CANVAS_ZOOM: 1,
  MIN_ZOOM: 0.1,
  MAX_ZOOM: 2,
  MINIMAP_POSITION: "bottom-right",
  MINIMAP_DEFAULT_WIDTH: 140,
  MINIMAP_DEFAULT_HEIGHT: 88,
  MINIMAP_NODE_BORDER_RADIUS: 4,
  MINIMAP_NODE_STROKE_WIDTH: 2,
  MINIMAP_NODE_HEIGHT: 8,
  MINIMAP_NODE_WIDTH: 14,
  MINIMAP_PANNABLE: true,
  MINIMAP_ZOOMABLE: true,
  NODE_DEFAULT_WIDTH: 180,
  NODE_DEFAULT_HEIGHT: 40,
  EDGE_VALIDATION_ENABLED: true,
  EXECUTION_STEP_DELAY_MS: 0,
  WORKFLOW_STORAGE_KEY: 'visual-workflow-canvas-state',
  WORKFLOW_AUTOSAVE_DEBOUNCE_MS: 700,
  WORKFLOW_SAVE_INDICATOR_MIN_VISIBLE_MS: 320,
  SERIALIZATION_FORMAT_VERSION: '1.0.0',
  URL_SHORTENER_MIN_LENGTH: 6,
  URL_SHORTENER_MAX_LENGTH: 15,
  URL_SHORTENER_DEFAULT_LENGTH: 8,
  URL_SHORTENER_MOCK_BASE_URL: 'https://short.url/',
  PERSIST_CANVAS_VIEWPORT: true,
  VALID_NODE_TYPES: [...Object.values(VALID_NODE_TYPES_MAP)] as const,
  NODE_TYPE_ALIASES: {
    DECISION: 'IFELSE',
  } as const,
} as const
