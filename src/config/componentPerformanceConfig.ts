export const HEAVY_COMPONENT_SPLIT_POLICY = {
  workflowCanvas: {
    chunkName: "workflow-canvas-chunk",
    loadTimeoutMilliseconds: 5000,
    fallbackDisplayDelayMilliseconds: 120,
  },
} as const;
